const divide = function (dividend,divisor) {
    return dividend / divisor;

}

console.log(divide(2000,100));

const square = (factor) => factor*factor;

const add = (number1, number2) => number1 + number2;

console.log (square(3));
console.log (add (4,7));

